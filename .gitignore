.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/

# Vendored/nested repos and benchmark sandboxes — not submodules, tracked separately.
/graphify-upstream/
/crg/crg-upstream/
/crg/sandbox-flask/
/bench/sandbox-a/
/bench/sandbox-big/
/bench/agentctx/repo/
/bench/agentctx/repo-express/
/bench/agentctx/repo-flask/
/bench/agentctx/wt/

# e2e test scratch dir (self-contained mini git repo, regenerated per test run)
/.e2e-tmp/

# Generated graph output. Rebuildable with `graphify extract .`, ~101 MB across
# 2,462 files, and the post-commit hook regenerates it after every commit — so
# tracking it meant the tree was dirty again the moment a commit landed.
/graphify-out/

# end-to-end fix eval scratch: per-task venvs and agent working copies
/bench/fixeval/venvs/
/bench/fixeval/work/
